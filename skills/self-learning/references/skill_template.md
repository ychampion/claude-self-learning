# Skill Template

Use this template when generating new skills. Copy and customize.

```markdown
---
name: <slugified-topic>
description: <One clear sentence about what this technology does and when to use it>
version: 1.0.0
sources_verified: <YYYY-MM-DD>
---

# <Topic Name>

<2-3 sentence overview of what this is and its primary use case>

## Quick Reference

| Item | Value |
|------|-------|
| Official Docs | [<name>](<URL>) |
| Installation | `pip install <pkg>` / `bun add <pkg>` |
| Auth Required | Yes/No |
| Primary Language | Python / TypeScript / Both |

## Installation

### Python

```bash
pip install <package>
```

### JavaScript/TypeScript

```bash
bun add <package>
# or
npm install <package>
```

## Authentication

<Clear instructions on how to set up authentication>

### Getting API Keys

1. Go to [<service dashboard>](<URL>)
2. Create an account or sign in
3. Navigate to API keys section
4. Generate a new key

### Configuration

```python
import os

# Recommended: Use environment variables
api_key = os.environ.get("<ENV_VAR_NAME>")
```

```typescript
// Environment variable (recommended)
const apiKey = process.env.<ENV_VAR_NAME>;
```

## Basic Usage

### Python

```python
"""
<Brief description of what this example does>
"""
from <package> import <Client>

# Initialize client
client = <Client>(api_key=os.environ.get("<ENV_VAR>"))

try:
    # Make request
    response = client.<method>(
        <param1>="<value>",
        <param2>="<value>"
    )

    # Process response
    print(response.<attribute>)

except <SpecificError> as e:
    print(f"API error: {e}")
except Exception as e:
    print(f"Unexpected error: {e}")
```

### TypeScript

```typescript
/**
 * <Brief description of what this example does>
 */
import { <Client> } from '<package>';

const client = new <Client>({
  apiKey: process.env.<ENV_VAR>
});

async function main() {
  try {
    const response = await client.<method>({
      <param1>: '<value>',
      <param2>: '<value>'
    });

    console.log(response.<attribute>);
  } catch (error) {
    if (error instanceof <SpecificError>) {
      console.error('API error:', error.message);
    } else {
      throw error;
    }
  }
}

main();
```

## Key Capabilities

### <Capability 1>

<Brief explanation of what this capability does>

```python
# <Capability 1> example
<code>
```

### <Capability 2>

<Brief explanation of what this capability does>

```python
# <Capability 2> example
<code>
```

## Best Practices

1. **<Practice Title>**: <Explanation of why this matters and how to implement it>

2. **<Practice Title>**: <Explanation of why this matters and how to implement it>

3. **<Practice Title>**: <Explanation of why this matters and how to implement it>

## Common Errors

| Error | Cause | Fix |
|-------|-------|-----|
| `<ErrorType>` | <Why this happens> | <How to fix it> |
| `<ErrorType>` | <Why this happens> | <How to fix it> |

## Additional Resources

- [Official Documentation](<url>)
- [API Reference](<url>)
- [GitHub Repository](<url>)

---
*Skill generated by claude-self-learning v1.0.0*
*Sources verified: <YYYY-MM-DD>*
```

## Template Variables

| Variable | Description | Example |
|----------|-------------|---------|
| `<slugified-topic>` | URL-safe topic name | `anthropic-api` |
| `<Topic Name>` | Human-readable title | `Anthropic API` |
| `<package>` | Package name for install | `anthropic` |
| `<Client>` | Main client class | `Anthropic` |
| `<ENV_VAR>` | Environment variable | `ANTHROPIC_API_KEY` |
| `<method>` | Primary API method | `messages.create` |
| `<SpecificError>` | API-specific error | `APIError` |

## Customization Notes

- Remove sections that don't apply (e.g., Auth for tools without authentication)
- Add sections for unique features (e.g., Streaming, Webhooks)
- Adjust language examples based on what's most relevant
- Keep total length between 2,000-4,000 tokens
