---
name: FastAPI Framework
description: Modern, fast (high-performance) web framework for building APIs with Python
version: 1.0.0
sources_verified: 2024-02-10
tags: ["api", "python", "web", "framework"]
---

# FastAPI Framework

Modern, fast (high-performance), web framework for building APIs with Python 3.7+ based on standard Python type hints.

## Quick Reference

| Item | Value |
|------|-------|
| Official Docs | [fastapi.tiangolo.com](https://fastapi.tiangolo.com) |
| Installation | `pip install fastapi uvicorn` |
| Auth Required | No |
| Primary Language | Python |

## Installation

### Python

```bash
pip install fastapi uvicorn[standard]
```

## Basic Usage

### Python

```python
"""
Basic FastAPI application
"""
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
async def root():
    return {"message": "Hello World"}

@app.get("/items/{item_id}")
async def read_item(item_id: int, q: str = None):
    try:
        return {"item_id": item_id, "q": q}
    except Exception as e:
        return {"error": str(e)}
```

Run with:
```bash
uvicorn main:app --reload
```

## Key Features

### Automatic API Documentation

FastAPI automatically generates interactive API documentation using Swagger UI and ReDoc.

### Type Hints and Validation

```python
from pydantic import BaseModel

class Item(BaseModel):
    name: str
    price: float
    is_offer: bool = False

@app.post("/items/")
async def create_item(item: Item):
    return item
```

### Async Support

```python
@app.get("/async-items/")
async def read_async_items():
    # Async database query
    items = await fetch_items_from_db()
    return items
```

## Advanced Usage

### Dependency Injection

```python
from fastapi import Depends

def get_current_user(token: str):
    # Verify token
    return {"user": "john"}

@app.get("/users/me")
async def read_users_me(current_user: dict = Depends(get_current_user)):
    return current_user
```

### Background Tasks

```python
from fastapi import BackgroundTasks

def send_email(email: str, message: str):
    # Send email logic
    pass

@app.post("/send-notification/")
async def send_notification(
    email: str,
    background_tasks: BackgroundTasks
):
    background_tasks.add_task(send_email, email, "Hello!")
    return {"message": "Notification sent"}
```

## Best Practices

1. **Use Type Hints**: Always use Python type hints for automatic validation and documentation
2. **Organize Routes**: Use `APIRouter` to organize endpoints into modules
3. **Handle Errors**: Implement proper exception handlers for consistent error responses
4. **Async/Await**: Use async functions for I/O-bound operations
5. **Security**: Use OAuth2 with Password (and hashing), JWT tokens for authentication

## Common Errors

| Error | Cause | Fix |
|-------|-------|-----|
| `ImportError: No module named 'fastapi'` | FastAPI not installed | `pip install fastapi` |
| `Port already in use` | Another process using port 8000 | Use different port: `uvicorn main:app --port 8001` |
| `ValidationError` | Invalid request data | Check Pydantic model matches request data |

## Resources

- [Official Documentation](https://fastapi.tiangolo.com)
- [GitHub Repository](https://github.com/tiangolo/fastapi)
- [Tutorial](https://fastapi.tiangolo.com/tutorial/)

---
*Skill generated by claude-self-learning v2.0.0*
*Sources verified: 2024-02-10*
